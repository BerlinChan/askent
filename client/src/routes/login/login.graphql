mutation Login($email: String!, $password: String!) {
  login(password: $password, email: $email) {
    token
    user {
      name
      email
    }
  }
}

# Use client-side schema
query GetToken {
  token @client
}

mutation Token($newToken: String!) {
  token(newToken: $newToken) @client
}
