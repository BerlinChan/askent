query EventsByMe($searchString: String) {
  eventsByMe(searchString: $searchString) {
    id
    shortId
    name
    code
    startAt
    endAt
  }
}

query CheckEventCodeExist($code: String!) {
  checkEventCodeExist(code: $code)
}

mutation CreateEvent(
  $code: String!
  $name: String!
  $startAt: DateTime!
  $endAt: DateTime!
) {
  createEvent(code: $code, name: $name, startAt: $startAt, endAt: $endAt) {
    id
    shortId
    code
    name
    createdAt
    updatedAt
    startAt
    endAt
  }
}

mutation DeleteEvent($eventId: ID!) {
  deleteEvent(eventId: $eventId) {
    id
    name
    code
  }
}
