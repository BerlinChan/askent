query Event($eventId: ID!) {
  event(eventId: $eventId) {
    id
    name
    code
    startAt
    endAt
    moderation
  }
}

mutation UpdateEvent(
  $eventId: ID!
  $code: String
  $name: String
  $startAt: DateTime
  $endAt: DateTime
  $moderation: Boolean
) {
  updateEvent(
    eventId: $eventId
    code: $code
    name: $name
    startAt: $startAt
    endAt: $endAt
    moderation: $moderation
  ) {
    id
    code
    name
    startAt
    endAt
    moderation
  }
}
