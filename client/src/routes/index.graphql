fragment UserInfo on User {
  id
  name
  email
  roles {
    id
    name
  }
}

query Me {
  me {
    ...UserInfo
  }
}

mutation UpdateUser($input: UpdateUserInput!) {
  updateUser(input: $input) {
    ...UserInfo
  }
}
