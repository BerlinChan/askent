mutation CreateQuestion($eventId: ID!, $content: String!) {
  createQuestion(eventId: $eventId, content: $content) {
    id
    createdAt
    updatedAt
    content
    published
    top
    voteCount
  }
}
